# Experiment outputs

Each run directory should contain the resolved config, Git commit, manifest and
split fingerprints, package versions, random seeds, checkpoint, metrics and
shortcut-baseline report. Large run outputs are ignored by Git.

