# Notebooks

Notebooks are for exploration and visualization only. Any logic needed to build
a manifest, split data, extract features, train a model or compute a reported
metric must live in `src/` and be covered by tests.

