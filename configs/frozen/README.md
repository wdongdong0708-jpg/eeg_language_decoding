# Frozen experiment protocols

Files in this directory are immutable experiment contracts. Their SHA-256
digests are recorded in a matching freeze manifest under `experiments/frozen/`.
Runtime overrides are restricted to the training seed, output directory, and
compute device. Any scientific change requires a new protocol version.
