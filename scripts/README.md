# Command-line stages

The filenames in this directory define the intended pipeline. Data audit and
manifest construction come before feature extraction and training. Scripts
remain thin wrappers; reusable logic belongs in `src/`.

